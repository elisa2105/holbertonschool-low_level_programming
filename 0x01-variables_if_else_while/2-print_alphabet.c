#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int firstChar = 0;
for (firstChar = 97 ; firstChar < 123 ; firstChar++)
putchar (firstChar);
putchar ('\n');
return (0);
}
