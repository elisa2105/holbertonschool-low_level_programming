#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int firstChar = 0;
for (; firstChar < 10 ; firstChar++)
printf("%d", firstChar);
printf("\n");
return (0);
}
